# Runtime data files — these contain personal chat/whisper history and
# per-user settings written by the addon at runtime. Never publish them.
data.lua
channels.lua
chan_*.lua
export_*.lua
stress_tmp.lua

# Editor / OS noise
*.bak
*.tmp
Thumbs.db
desktop.ini
